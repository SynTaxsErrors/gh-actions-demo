name: helloworld

on:
  push:
   branches: [main]
jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Run a one-line script
        run: echo "Hello GitHub Actions."
 Seccond-job: 
     runs-on: windows-latest
     steps:
      - rub: echo "This job runnig on ${{runner.os}}"
